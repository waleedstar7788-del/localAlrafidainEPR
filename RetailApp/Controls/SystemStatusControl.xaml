<UserControl x:Class="RetailApp.Controls.SystemStatusControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="Transparent">
             
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Border Background="{DynamicResource SurfaceHighlightBrush}" CornerRadius="8" Padding="16,12" Margin="0,0,0,12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="{Binding Name}" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="14" VerticalAlignment="Center"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBlock Text="{Binding Status}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="13" FontWeight="Medium" VerticalAlignment="Center" Margin="0,0,12,0"/>
                
                <!-- Simulating Green Dot for Healthy -->
                <Border Width="10" Height="10" CornerRadius="5" Background="{DynamicResource BrandSuccessBrush}" VerticalAlignment="Center" 
                        Visibility="{Binding IsHealthy, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Border.Effect>
                        <DropShadowEffect Color="#10B981" BlurRadius="6" ShadowDepth="0" Opacity="0.6"/>
                    </Border.Effect>
                </Border>
                
                <!-- Simulating Red Dot for Unhealthy (inverted logic for simple UI if needed) -->
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
