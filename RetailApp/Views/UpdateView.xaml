<UserControl x:Class="RetailApp.Views.UpdateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             FlowDirection="RightToLeft">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="تحديثات النظام" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
            <TextBlock Text="التحقق من الإصدارات الجديدة وترقية النظام" FontSize="14" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Version Card -->
            <Border Grid.Column="0" Style="{StaticResource KpiCard}" Margin="0,0,12,0" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="الإصدار الحالي" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,16"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,24" HorizontalAlignment="Center">
                        <materialDesign:PackIcon Kind="InformationOutline" Width="32" Height="32" Foreground="{DynamicResource BrandPrimaryBrush}" Margin="0,0,16,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding CurrentVersion}" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Button Command="{Binding CheckForUpdatesCommand}" Style="{StaticResource ModernPrimaryButton}" Content="التحقق من وجود تحديثات" Height="45"/>
                    <ProgressBar IsIndeterminate="{Binding IsChecking}" Visibility="{Binding IsChecking, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,16,0,0"/>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource BrandSuccessBrush}" HorizontalAlignment="Center" Margin="0,16,0,0" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>

            <!-- Update Info Card -->
            <Border Grid.Column="1" Style="{StaticResource KpiCard}" Margin="12,0,0,0" VerticalAlignment="Top" 
                    Visibility="{Binding IsUpdateAvailable, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
                <StackPanel>
                    <TextBlock Text="تفاصيل وإضافات التحديث الجديد" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,16"/>
                    
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="رقم الإصدار:" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        <TextBlock Grid.Column="1" Text="{Binding AvailableUpdate.Version}" FontWeight="Bold" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                    </Grid>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="تاريخ الإصدار:" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        <TextBlock Grid.Column="1" Text="{Binding AvailableUpdate.ReleaseDate, StringFormat=yyyy-MM-dd}" Foreground="{DynamicResource TextPrimaryBrush}"/>
                    </Grid>

                    <TextBlock Text="الإضافات والتحسينات الجديدة:" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                    <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,16">
                        <TextBox Text="{Binding AvailableUpdate.ReleaseNotes}" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" Height="110" 
                                 BorderThickness="0" Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="13"/>
                    </Border>

                    <Button Command="{Binding DownloadUpdateCommand}" IsEnabled="{Binding IsDownloading, Converter={StaticResource InvertBooleanConverter}}" Style="{StaticResource ModernPrimaryButton}" Content="تحميل وتثبيت التحديث الآن" Height="45"/>
                    
                    <!-- Real-time Download Progress Bar -->
                    <StackPanel Visibility="{Binding IsDownloading, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,16,0,0">
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="جاري التحميل المباشر..." FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <TextBlock Text="{Binding ProgressText}" HorizontalAlignment="Left" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                        </Grid>
                        <ProgressBar Value="{Binding DownloadProgress}" Maximum="100" Height="8" IsIndeterminate="False" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
